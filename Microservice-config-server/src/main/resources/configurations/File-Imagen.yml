

spring:
  application:
    name: File-Imagen
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campofutbol
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: create
      database: mysql
      database-platform: org.hibernate.dialect.MySQL8Dialect

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB


eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8087

project:
  poster: File-Imagen/my_image/

base:
  url: "http://localhost:8080"
